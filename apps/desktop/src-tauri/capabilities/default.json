{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Default desktop capability for local shell operations.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:default"]
}
